all:
  hosts:
    vps:
      ansible_host: "{{ lookup('env', 'VAULT_SERVER_IP') }}"
      ansible_user: "{{ lookup('env', 'VAULT_DEPLOY_USER') }}"
      ansible_ssh_private_key_file: ~/.ssh/id_rsa
      ansible_python_interpreter: /usr/bin/python3
